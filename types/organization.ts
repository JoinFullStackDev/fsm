/**
 * Organization type definitions
 * Re-exported from lib/organizationContext for convenience
 */

export type { Organization, PackageFeatures, Package, Subscription, OrganizationContext } from '@/lib/organizationContext';

